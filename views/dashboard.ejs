<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | DSH에듀</title>
  <meta name="description" content="<%= description %>">
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  
  <!-- AOS Animation Library -->
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
  
  <!-- Swiper CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.css" />
  
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <!-- Header -->
  <%- include('./partials/header') %>

  <div class="container mt-5 pt-5">
    <div class="row">
      <div class="col-12">
        <!-- 대시보드 헤더 -->
        <div class="bg-primary text-white rounded p-4 mb-4">
          <div class="d-flex align-items-center">
            <div class="me-3">
              <i class="fas fa-tachometer-alt fa-3x"></i>
            </div>
            <div>
              <h1 class="mb-0" data-i18n="welcomeBack">환영합니다!</h1>
              <p class="mb-0 fs-5"><%= user.name %>님의 대시보드</p>
            </div>
          </div>
        </div>

        <!-- 빠른 액션 카드들 -->
        <div class="row g-4 mb-5">
          <div class="col-md-4">
            <div class="card h-100 shadow-sm border-0">
              <div class="card-body text-center">
                <div class="mb-3">
                  <i class="fas fa-user fa-3x text-primary"></i>
                </div>
                <h5 class="card-title" data-i18n="myProfile">내 프로필</h5>
                <p class="card-text text-muted" data-i18n="profileDesc">개인정보 및 계정 설정을 관리하세요</p>
                <a href="/dashboard/profile" class="btn btn-primary">
                  <i class="fas fa-edit me-2"></i><span data-i18n="edit">편집</span>
                </a>
              </div>
            </div>
          </div>

          <div class="col-md-4">
            <div class="card h-100 shadow-sm border-0">
              <div class="card-body text-center">
                <div class="mb-3">
                  <i class="fas fa-list-alt fa-3x text-success"></i>
                </div>
                <h5 class="card-title" data-i18n="myEnrollments">내 등록 현황</h5>
                <p class="card-text text-muted" data-i18n="enrollmentsDesc">등록한 프로그램 현황을 확인하세요</p>
                <a href="/dashboard/enrollments" class="btn btn-success">
                  <i class="fas fa-eye me-2"></i><span data-i18n="view">확인</span>
                </a>
              </div>
            </div>
          </div>

          <div class="col-md-4">
            <div class="card h-100 shadow-sm border-0">
              <div class="card-body text-center">
                <div class="mb-3">
                  <i class="fas fa-book fa-3x text-info"></i>
                </div>
                <h5 class="card-title" data-i18n="programs">프로그램</h5>
                <p class="card-text text-muted" data-i18n="programsDesc">새로운 프로그램을 찾아보세요</p>
                <a href="/programs" class="btn btn-info">
                  <i class="fas fa-search me-2"></i><span data-i18n="browse">둘러보기</span>
                </a>
              </div>
            </div>
          </div>
        </div>

        <% if (user.role === 'admin') { %>
        <!-- 관리자 전용 섹션 -->
        <div class="row g-4 mb-5">
          <div class="col-12">
            <div class="card border-warning">
              <div class="card-header bg-warning text-dark">
                <h5 class="mb-0">
                  <i class="fas fa-shield-alt me-2"></i>
                  <span data-i18n="adminSection">관리자 도구</span>
                </h5>
              </div>
              <div class="card-body">
                <div class="row">
                  <div class="col-md-6">
                    <a href="/admin" class="btn btn-warning btn-lg w-100 mb-3">
                      <i class="fas fa-cogs me-2"></i>
                      <span data-i18n="adminPanel">관리자 패널</span>
                    </a>
                  </div>
                  <div class="col-md-6">
                    <a href="/posts" class="btn btn-outline-warning btn-lg w-100 mb-3">
                      <i class="fas fa-comments me-2"></i>
                      <span data-i18n="community">커뮤니티 관리</span>
                    </a>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
        <% } %>

        <!-- 최근 활동 -->
        <div class="row">
          <div class="col-12">
            <div class="card shadow-sm border-0">
              <div class="card-header bg-light">
                <h5 class="mb-0">
                  <i class="fas fa-clock me-2 text-muted"></i>
                  <span data-i18n="recentActivity">최근 활동</span>
                </h5>
              </div>
              <div class="card-body">
                <div class="text-center text-muted py-4">
                  <i class="fas fa-clock fa-2x mb-3"></i>
                  <p data-i18n="noRecentActivity">최근 활동이 없습니다.</p>
                  <small class="text-muted" data-i18n="activityWillAppear">활동 내역이 여기에 표시됩니다.</small>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <%- include('./partials/footer') %>
  
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  
  <!-- AOS Animation Library -->
  <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
  
  <!-- Swiper JS -->
  <script src="https://cdn.jsdelivr.net/npm/swiper@8/swiper-bundle.min.js"></script>
  
  <!-- Translations JS -->
  <script src="/js/translations.js"></script>
  
  <!-- Custom JS -->
  <script src="/js/main.js"></script>
  
  <!-- Initialize AOS -->
  <script>
    AOS.init({
      duration: 800,
      easing: 'ease-in-out',
      once: true
    });
  </script>

  <!-- AOS 애니메이션 추가 -->
  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // 카드 애니메이션
      const cards = document.querySelectorAll('.card');
      cards.forEach((card, index) => {
        card.style.opacity = '0';
        card.style.transform = 'translateY(20px)';
        setTimeout(() => {
          card.style.transition = 'all 0.5s ease';
          card.style.opacity = '1';
          card.style.transform = 'translateY(0)';
        }, index * 100);
      });
    });
  </script>
</body>
</html> 